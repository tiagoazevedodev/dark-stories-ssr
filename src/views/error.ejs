<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro - Black Stories</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <!-- Header -->
    <nav class="bg-gray-800 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="text-2xl font-bold text-purple-400">
                        <i class="fas fa-mask mr-2"></i>Black Stories
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <% if (user) { %>
                        <a href="/dashboard" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">
                            <i class="fas fa-dashboard mr-2"></i>Dashboard
                        </a>
                        <% if (user.isSubscriber) { %>
                            <a href="/history" class="text-purple-400 hover:text-purple-300 px-3 py-2 rounded-md">
                                <i class="fas fa-history mr-2"></i>Histórico
                            </a>
                            <a href="/create" class="bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded-md">
                                <i class="fas fa-plus mr-2"></i>Criar
                            </a>
                        <% } else { %>
                            <a href="/premium" class="bg-yellow-600 hover:bg-yellow-700 px-4 py-2 rounded-md">
                                <i class="fas fa-crown mr-2"></i>Premium
                            </a>
                        <% } %>
                        <form action="/logout" method="POST" class="inline">
                            <button type="submit" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">
                                <i class="fas fa-sign-out-alt mr-2"></i>Sair
                            </button>
                        </form>
                    <% } else { %>
                        <a href="/login" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">
                            <i class="fas fa-sign-in-alt mr-2"></i>Entrar
                        </a>
                        <a href="/register" class="bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded-md">
                            <i class="fas fa-user-plus mr-2"></i>Cadastrar
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center">
            <!-- Erro Icon -->
            <div class="mx-auto flex items-center justify-center h-32 w-32 rounded-full bg-red-100 mb-8">
                <i class="fas fa-exclamation-triangle text-6xl text-red-600"></i>
            </div>
            
            <!-- Error Title -->
            <h1 class="text-4xl font-bold text-red-400 mb-4">Oops! Algo deu errado</h1>
            
            <!-- Error Message -->
            <div class="bg-gray-800 border border-red-500 rounded-lg p-6 mb-8 max-w-lg mx-auto">
                <p class="text-red-300 text-lg mb-2">
                    <i class="fas fa-info-circle mr-2"></i>Mensagem de erro:
                </p>
                <p class="text-gray-300 break-words">
                    <%= message || 'Erro interno do servidor' %>
                </p>
            </div>
            
            <!-- Navigation Options -->
            <div class="space-y-4 sm:space-y-0 sm:space-x-4 sm:flex sm:justify-center">
                <a href="/" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-purple-600 hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500 transition-colors">
                    <i class="fas fa-home mr-2"></i>
                    Voltar ao Início
                </a>
                
                <button onclick="history.back()" class="inline-flex items-center px-6 py-3 border border-gray-600 text-base font-medium rounded-md text-gray-300 bg-gray-800 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Voltar à Página Anterior
                </button>
            </div>
            
            <!-- Additional Help -->
            <div class="mt-12 text-gray-400">
                <p class="text-sm">
                    Se o problema persistir, entre em contato com o suporte ou tente novamente mais tarde.
                </p>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 border-t border-gray-700 mt-auto">
        <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="text-gray-400">
                    © 2024 Black Stories. Todos os direitos reservados.
                </p>
            </div>
        </div>
    </footer>
</body>
</html> 